# Only tells the client where the config server is.
# Everything else (port, eureka, resilience4j, tracing) is served centrally.
spring:
  application:
    name: order-service
  config:
    import: optional:configserver:http://localhost:8888
